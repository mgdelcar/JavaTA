<%= form_for(@problem_submission) do |f| %>
  <% if @problem_submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@problem_submission.errors.count, "error") %> prohibited this problem submission from being saved:</h2>

      <ul>
      <% @problem_submission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :problem_id %>

  <div class="field">
    <%= f.label :file_relative_path %><br>
    <%= f.text_field :file_relative_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
