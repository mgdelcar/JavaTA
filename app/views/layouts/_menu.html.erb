<div class="menu_bar">
  <%= render partial: "layouts/#{current_user.user_type}_menu" unless current_user.nil? %>
  <div class="menu_button menu_right_button">
    <%= if logged_in? then link_to('Log out', '/logout') else link_to('Log in', '/login') end %>
  </div>
</div>